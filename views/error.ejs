<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error <%= err && err.statusCode ? err.statusCode : 500 %> | MNIT Student Hub</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        .error-container {
            min-height: 70vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
        }
        .error-icon {
            font-size: 6rem;
            color: #dc3545;
            margin-bottom: 1rem;
        }
        .error-code {
            font-size: 5rem;
            font-weight: bold;
            color: #212529;
            margin: 0;
            line-height: 1;
        }
        .error-message {
            font-size: 1.5rem;
            color: #6c757d;
            margin: 1rem 0;
        }
        .error-details {
            background-color: #f8f9fa;
            padding: 1rem;
            border-radius: 0.5rem;
            margin: 1.5rem 0;
            max-width: 700px;
            width: 100%;
            text-align: left;
        }
        .error-stack {
            font-family: monospace;
            font-size: 0.9rem;
            color: #6c757d;
            white-space: pre-wrap;
            overflow-x: auto;
        }
        .error-actions .btn {
            margin: 0.5rem;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">


    <!-- Error Section -->
    <main class="flex-grow-1">
        <div class="error-container">
            <i class="fas fa-exclamation-triangle error-icon"></i>
            <h1 class="error-code"><%= err && err.statusCode ? err.statusCode : 500 %></h1>
            <p class="error-message"><%= err && err.message ? err.message : 'Something went wrong.' %></p>

            <% if (process.env.NODE_ENV === 'development' && err && err.stack) { %>
                <div class="error-details">
                    <h5>Error Details:</h5>
                    <pre class="error-stack"><%= err.stack %></pre>
                </div>
            <% } %>

            <div class="error-actions">
                <a href="javascript:history.back()" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i> Go Back
                </a>
                <a href="/" class="btn btn-primary">
                    <i class="fas fa-home"></i> Go Home
                </a>
                <% if (err && err.statusCode === 404) { %>
                    <a href="/search" class="btn btn-outline-primary">
                        <i class="fas fa-search"></i> Search
                    </a>
                <% } %>
            </div>
        </div>
    </main>

   
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
